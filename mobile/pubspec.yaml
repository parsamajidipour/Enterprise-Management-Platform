name: emp_platform_mobile
description: Enterprise Management Platform mobile app for technician field execution.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^9.2.2
  geolocator: ^13.0.2
  http: ^1.2.2
  image_picker: ^1.1.2
  intl: ^0.20.1
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
